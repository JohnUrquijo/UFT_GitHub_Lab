name: Probar Runner UFT

on:
  workflow_dispatch:

jobs:
  test-runner:
    runs-on: self-hosted

    steps:
      - name: Validar maquina
        run: |
          hostname
          whoami
          echo "Runner funcionando correctamente"
